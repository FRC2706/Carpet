<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mergerobotics.memo">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".backend.App"
        android:allowBackup="true"
        android:icon="@mipmap/memo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/memo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.SettingsGUI"></activity>
        <activity android:name=".gui.Endgame" />
        <activity
            android:name=".gui.Test"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.TeleopActivity2019"
            android:label="@string/title_activity_teleop_2019"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.SandstormActivity"
            android:label="@string/title_activity_sandstorm"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".gui.PrematchActivity" />
        <activity
            android:name=".gui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>